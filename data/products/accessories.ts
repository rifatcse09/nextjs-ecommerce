import { Product } from "@/types/product";
import { products as fashionData } from "./fashion";

export const catAccessories: Product[] = fashionData.slice(0, 4);
export const allAccessories: Product[] = [];
